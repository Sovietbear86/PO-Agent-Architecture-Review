"""Trusted built-in/plugin namespace for Agent Core V4.

Only modules in this application-owned package are auto-discovered by the V4
plugin registry. User filesystem paths are never imported as runtime plugins.
"""
